<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.frogobox.faisalamirprofile">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_icon_amir"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_icon_amir"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activity.SocialMediaActivity"></activity>
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/AppThemeNoToolbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>